BEFUND

Die Roadmap `atlas-blaupause.md` enthält noch den inkonsistenten Status `[~]` für FTS, obwohl in der Memory-Regel `[~]` verboten ist und durch `[ ] Task (teilweise: ...)` ersetzt werden soll.
Außerdem wurden offene Features wie "line_count", "encoding", "mime_type" in der Roadmap als `[ ] teilweise gehärtet` markiert, obwohl sie teilweise wieder ausgebaut wurden (wie in den Kommentaren dort vermerkt) bzw. im Repo als heuristische Versuche in `atlas.py` existieren.
- FTS-Suche in `search.py` fehlt (es wird noch lineares `inventory.jsonl`-Scannen durchgeführt, belegt durch `merger/lenskit/atlas/search.py`).
- FTS-Evaluierung in `chunks_fts` existiert (`merger/lenskit/retrieval/index_db.py`).
- Cross-root growth reports fehlen `[ ] Cross-root growth reports definieren`.

STATUSMATRIX
Phase 4:
- [ ] SQLite-FTS evaluieren und festziehen (teilweise: FTS5 technologisch inChunks bestätigt, aber in Atlas-Suche unvollständig)
- [x] Metadaten-Suchschema definieren
- [x] Path-Search implementieren
...

PLANPRÜFUNG
Context7 zeigt, dass FTS5 performant ist und für Lenskit-Chunks läuft. Das Repo (search.py) hinkt architektonisch hinterher (nur linearer Scan). Die Entscheidung für die Integration (inline vs. derive, etc.) gemäß `atlas-fts-integration.md` steht noch aus.

NÄCHSTER SCHRITT
Bereinigung des `[~]` Markers in der Blaupause gemäß den Memory-Richtlinien, da GitHub-inkompatible Erweiterungen vermieden werden sollen und "teilweise" explizit als solches benannt werden muss. Die reine Dokumentationsanpassung sichert die epistemische Wahrheit ohne verfrühten Code einzuziehen.

TARGET PROOF
Datei: `docs/atlas-blaupause.md`, Zeile 887.
Aktueller Zustand vor Änderung: `- [~] SQLite-FTS evaluieren und festziehen`
Warum Änderung nötig: Der Marker `[~]` verletzt das Markdown-Format und täuscht einen proprietären Zustand vor. Er muss durch `[ ] ... (teilweise: ...)` ersetzt werden.

UMSETZUNG
Der Status von "SQLite-FTS" in `docs/atlas-blaupause.md` wurde angepasst.

VERIFIKATION
`grep "SQLite-FTS" docs/atlas-blaupause.md` zeigt den bereinigten, zulässigen `[ ] SQLite-FTS evaluieren und festziehen (teilweise: FTS5 technologisch inChunks bestätigt, aber in Atlas-Suche unvollständig)` Status.
